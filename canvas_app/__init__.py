"""
Canvas Accuracy — Canvas New Quizzes reporting, MCAT exam growth, and Nova
attendance, merged into the Institution TJ dashboard.

The views live in canvas_app.views and are dispatched from main.py's Canvas
radio. Each exposes a single render() so importing this package has no
Streamlit side effects.
"""
